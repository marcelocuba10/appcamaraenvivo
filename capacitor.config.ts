import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.quetalelpuente.ggcode',
  appName: '¿Qué tal el puente?',
  webDir: 'www'
};

export default config;
